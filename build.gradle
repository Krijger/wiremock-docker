buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath group: 'nl.qkrijger.gradle', name: 'docker-gradle', version: '0.2'
  }
}
apply plugin: 'groovy'
apply plugin: 'docker'


docker.imageName = 'qkrijger/wiremock'
